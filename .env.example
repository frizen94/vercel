# Exemplo de configuração de variáveis de ambiente
# Copie este arquivo para .env e configure as variáveis apropriadas

# Configuração do Banco de Dados
# Para desenvolvimento local (SQLite):
# DATABASE_URL=file:./dev.db

# Para PostgreSQL local:
# DATABASE_URL=postgresql://usuario:senha@localhost:5432/kanban_db

# Para PostgreSQL na nuvem (Railway, Neon, etc.):
# DATABASE_URL=postgresql://usuario:senha@host:5432/database

# Para Railway (obtido automaticamente via variáveis de ambiente):
# DATABASE_URL será definido automaticamente pelo Railway
# Caso precise definir manualmente:
# DATABASE_URL=postgresql://postgres:senha@railway-host:5432/railway

# Configuração de Sessão (obrigatório)
SESSION_SECRET=minha-chave-secreta-super-forte-123456789

# Ambiente de execução
NODE_ENV=development

# Porta do servidor (opcional, padrão: 5000)
PORT=5000